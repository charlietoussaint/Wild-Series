{% extends 'base.html.twig' %}

{% block title %}Episode #{{ episode.number }}
	de saison
	{{ season.number }}
	de
	{{ program.title }}
{% endblock %}

{% block body %}
	<div class="media-body">
		<h1 class="mt-0">{{ program.title }}</h1>
		<div>
			<h2>
				Saison :{{ season.number }}
			</h2>
			<h3>
				Episode :{{ episode.number }}
			</h3>
			<p>
				Titre:{{ episode.title }}</p>
			<p>
				Description:{{ episode.synopsis }}</p>
		</div>

	</div>
</div>{% endblock %}
